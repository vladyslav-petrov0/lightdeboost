import React from "react";
import PropTypes from "prop-types";

import styles from "./Range.module.scss";

const ProductRange = () => {
  return <span>ProductRange</span>;
};

ProductRange.propTypes = {};

export default ProductRange;
